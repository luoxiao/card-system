<?php
namespace App\Http\Controllers\Admin; use App\Library\Response; use App\Order; use Illuminate\Http\Request; use App\Http\Controllers\Controller; class Dashboard extends Controller { function index(Request $spdd9f33) { $sp00d345 = array('today' => array('count' => 0, 'income' => 0, 'profit' => 0), 'yesterday' => array('count' => 0, 'income' => 0, 'profit' => 0)); $spbbd804 = Order::whereUserId(\Auth::Id())->whereDate('paid_at', \Carbon\Carbon::now()->toDateString())->where(function ($sp26c3c2) { $sp26c3c2->where('status', Order::STATUS_PAID)->orWhere('status', Order::STATUS_SUCCESS); })->selectRaw('COUNT(*) as `count`,SUM(`income`) as `income`,SUM(`income`-`cost`) as `profit`')->get()->toArray(); $sp67e04b = Order::whereUserId(\Auth::Id())->whereDate('paid_at', \Carbon\Carbon::yesterday()->toDateString())->where(function ($sp26c3c2) { $sp26c3c2->where('status', Order::STATUS_PAID)->orWhere('status', Order::STATUS_SUCCESS); })->selectRaw('COUNT(*) as `count`,SUM(`income`) as `income`,SUM(`income`-`cost`) as `profit`')->get()->toArray(); if (isset($spbbd804[0]) && isset($spbbd804[0]['count'])) { $sp00d345['today'] = array('count' => (int) $spbbd804[0]['count'], 'income' => (int) $spbbd804[0]['income'], 'profit' => (int) $spbbd804[0]['profit']); } if (isset($sp67e04b[0]) && isset($sp67e04b[0]['count'])) { $sp00d345['yesterday'] = array('count' => (int) $sp67e04b[0]['count'], 'income' => (int) $sp67e04b[0]['income'], 'profit' => (int) $sp67e04b[0]['profit']); } $sp00d345['need_ship_count'] = Order::whereUserId(\Auth::Id())->where('status', Order::STATUS_PAID)->count(); return Response::success($sp00d345); } function clearCache() { if (function_exists('opcache_reset')) { opcache_reset(); } return Response::success(); } }